# Project identity

`cli-agent` is a general-purpose Agent that completes tasks through CLI
operations.

# Commit request
Commit message format. We follow the Conventional Commits specification. This keeps commit history readable.
```
<type>(<scope>): <subject>
```

# References

- **`docs/handoff.md`** — Current implementation status, completed work, known limits, and the recommended next milestone.
- **`../CONTEXT.md`** — Ubiquitous language / glossary for the AEP-native Agent Runtime project. Defines every domain term with usage and "Avoid" notes.
- **`../.scratch/aep-native-agent-runtime/issues/`** — Decision tickets from the architecture-design phase (wayfinder). Each ticket resolves a design question toward the implementation-ready spec.
- **`../.scratch/aep-native-agent-runtime/map.md`** — Wayfinder map tracking the destination, decisions made, what's still open, and what's out of scope for the architecture effort.
- **`../.scratch/cli-agent-runtime/issues/`** — Implementation tickets for assembling the user-facing cli-agent from the Agent Runtime, sequenced as tracer-bullet milestones.
